Options -Indexes

RewriteEngine On
RewriteBase /

# Serve static files directly (directories are never browsable; always route through index.php)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything else to index.php
RewriteRule ^ index.php [L]
